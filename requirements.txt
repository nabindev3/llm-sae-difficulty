# Install-time floors — broad enough to `pip install`. These do NOT pin the
# numerics-critical stack, so a fresh resolve may shift the AUROC bootstrap CIs.
# To reproduce the committed numbers (AUROC=0.716 gate), use the exact pins:
#     pip install -r requirements.lock && pip install -e . --no-deps
torch>=2.4.0
transformers>=4.40.0
datasets>=2.19.0
safetensors>=0.4.0
scikit-learn>=1.4.0
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
matplotlib>=3.8.0
pyarrow>=14.0.0
tqdm>=4.66.0
statsmodels>=0.14.0
